mkdir mathlearn_project
cd mathlearn_project
mkdir -p src/{models,data,utils,api,tests}
mkdir -p data/{raw,processed,interim}
mkdir docs

touch .env
touch requirements.txt
touch README.md
touch src/__init__.py

echo "
DATABASE_URL=postgresql://username:password@localhost:5432/mathlearn
MODEL_PATH=./models
DATA_PATH=./data
DEBUG=True
" > .env